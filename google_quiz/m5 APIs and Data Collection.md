1.
Question 1

What are the three parts of a response message?

HTTP headers, blank line, and body 

Start or status line, header, and body 

Bookmarks, history, and security 

Encoding, body, and cache 
 
2.
Question 2

What does the line of code “table_row = table.find_all(name='tr')” do in web scraping?

It locates all the data within the table marked with a tag “h1” 

It locates all the data within the table marked with a tag “p” 

It locates all the data within the table marked with a tag “tr” 

It locates all the data within the table marked with a tag “a” 
 
3.
Question 3

A programmer is extracting cryptocurrency data using the PyCoinGecko API. When receiving the response, what format conversion is needed to work with the data as a Python dictionary?

Apply the text attribute to the response object F

Apply the json() method to the response object 

Apply the csv() method to the response object 

Apply the xml() method to the response object 
 
4.
Question 4

When creating a candlestick chart for cryptocurrency prices, what data processing step was used to prepare the time-based data?

Using the pandas to_datetime function to convert UNIX timestamps to readable dates 

Using the BeautifulSoup parser to extract time values 

Using the matplotlib date formatter to display only weekdays 

Using the NumPy time function to calculate moving averages 
 
5.
Question 5

How do HTML table elements relate to each other in the document tree structure?

The <table> element is the parent of <tr> elements, which are parents of <td> elements. 

The <td> element is the parent of <tr> elements, which are parents of <table> elements. 

Table elements are not hierarchically related but are all siblings. 

The <tr> element is the parent of both <td> and <table> elements. 
 
6.
Question 6

A developer is creating a web form that submits sensitive user information. Which HTTP method should they use for the form submission and why?

POST method because it sends data in the request body rather than in the URL

PUT method because it automatically encrypts the form data 

GET method because it allows faster transmission of large datasets 

DELETE method because it removes sensitive data after transmission 
 
7.
Question 7

What distinguishes REST APIs from other types of APIs?

They are restricted to local communication within a single application. 

They require specialized hardware to function. 

They can only be implemented in Python. 

They communicate through the internet using HTTP methods and typically exchange data in formats like JSON. 
 
8.
Question 8

What is the difference between CSV, JSON, and XML file formats?

CSV can only store numerical data; JSON can only store text data; XML can only store binary data. 

CSV is encrypted data; JSON is compressed data; XML is raw text data. 

CSV is for Windows systems; JSON is for Mac systems; XML is for Linux systems. 

CSV is tabular data with values separated by commas; JSON is a key-value format similar to Python dictionaries; XML uses hierarchical tags with attributes. 
 
9.
Question 9

A developer is creating an application that needs to retrieve data from a REST API. Which code snippet correctly sends a GET request with parameters?

import requests; r = requests.get('https://api.example.com/data?name=John&ID=123', data={'secure': True}) 

import requests; r = requests.post('https://api.example.com/data', headers={'name': 'John', 'ID': '123'}) 

import requests; r = requests.get('https://api.example.com/data', json={'name': 'John', 'ID': '123'}) 

import requests; payload = {'name': 'John', 'ID': '123'}; r = requests.get('https://api.example.com/data', params=payload) 
 
10.
Question 10

What is the first step in creating a BeautifulSoup object for web scraping?

Create a function to extract all links from the webpage 

Import BeautifulSoup and parse the HTML content 

Initialize a database to store the scraped data 

Define CSS selectors to identify content 