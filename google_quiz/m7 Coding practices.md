1.
Question 1

A smartwatch application displays the daily weather forecast without collecting any weather information. How does the application access this information?


By leveraging prior loaded information saved during installation.

By connecting to a weather service through an API.

By loading forecast data directly from the browser interface.

By observing past activity to estimate the weather forecast in the present.

2.
Question 2

Michel has written the function that calculates the total room charges for the room reservation application for a hotel. Before integrating this function with other website components, he wants to verify this function for its functionality. Which type of testing should Michel perform?


Unit testing

Integration testing

Performance testing

User testing

3.
Question 3

A developer wants to verify that a script follows Python coding standards. What is PyLint used for in this scenario?


PyLint is a Python IDE.

PyLint is a Python code compiler.

PyLint is a Python-based package creation tool.

PyLint is a Python static code analysis tool.

4.
Question 4

Which of the following is the right way to name constants based on the PEP8 guidelines?


Date_Of_Birth

DATE_OF_BIRTH

date_of_birth

dateOfBirth

5.
Question 5

When gathering requirements for an app to manage events, the customer mentions that the project aims to improve customer retention. Which of the following requirements describes the above scenario?


Technical requirements

Business requirements

User requirements

Constraints

6.
Question 6

Rocky is working on a Python project with various functions, where he wants to verify the correctness by organizing their test efficiently. Which of the following is the primary advantage of placing all test functions into a dedicated unit testing class?


To call unit tests from a single class object

To add one or more assertion methods 

To check if functions are returning the correct values

To import the unittest library

7.
Question 7

Which of the following could be one of your assumptions, while running a test on the square function using the input 2 for the test output “AssertionError: 8!=4”?


The test function was missing, so the unit test didn't run.

The function returned an incorrect value, causing the test to fail.

The assertEqual method was incorrectly used in the script.

The test logic was correct, but the output doesn't match as expected.

8.
Question 8

Consider the following code in the file my_code.py, in a directory named mypackage with __init__.py.

def print_hello():

    print("Hello")

Which of the following statements is the right way to access the function in the method?

1. 
    import my_code

    print_hello()
2. 
    from mypackage import my_code

    my_code.print_hello()
3. 
    from mypackage import print_hello

    print_hello()
4. 
    from mypackage.my_code import print_code

    print_hello()


9.
Question 9

You are writing a test for a specific function within a custom package module. Which of the following is the correct format to call that function with a parameter?


{package_name}. {module_name}. {function_name} (parameters)

run {package_name}. {module_name}. {function_name} (parameters)

{package_name} (parameters)

import {package_name}

10.
Question 10

You want to import contents from the “utilities” folder that includes several Python scripts and an __init__.py file into another script. Which option correctly describes what the “utilities” folder represents?


A structured package folder with modules and an __init__.py file. 

A single Python script statement and functions. 

A collection of Python scripts with no package structure.

A collection of scripts with several external dependencies. 